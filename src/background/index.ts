chrome.runtime.onInstalled.addListener(() => {
    console.log('Applyly™ extension installed');
});

// We can add more background logic here if needed for cross-tab communication
// or periodic syncs.
